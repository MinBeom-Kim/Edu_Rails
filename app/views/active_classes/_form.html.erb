<%= form_with(model: active_class, local: true) do |form| %>
  <% if active_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(active_class.errors.count, "error") %> prohibited this active_class from being saved:</h2>

      <ul>
      <% active_class.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :class_name %>
    <%= form.text_field :class_name %>
  </div>

  <div class="field">
    <%= form.label :max_number %>
    <%= form.text_field :max_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
